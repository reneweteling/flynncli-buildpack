#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e

BUILD_DIR=$1
CACHE_DIR=$2

L=/usr/local/bin/flynn

curl -sSL -A "`uname -sp`" https://dl.flynn.io/cli $CACHE_DIR/flynn
zcat $CACHE_DIR/flynn >$L 
chmod +x $L