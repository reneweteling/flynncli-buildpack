#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e

BUILD_DIR=$1
CACHE_DIR=$2

BIN_PATH="$BUILD_DIR/bin"
FLYNN_PATH="$BIN_PATH/flynn"

curl -sSL -A "`uname -sp`" https://dl.flynn.io/cli $FLYNN_PATH
chmod +x $FLYNN_PATH